= CustomizeAdmin

== Installation

=== Rails 3

Add to your Gamefile:

  gem 'customize_admin'

And then execute:

  bundle install

After this run install generator:

  rails g customize_admin:install
